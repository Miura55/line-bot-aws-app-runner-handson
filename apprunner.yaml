version: 1.0
runtime: go1.x
build:
  commands:
    - go mod download
    - go mod vendor
    - go build -o bin/app
run:
  command: bin/app
  port: 8080
  healthCheck:
    path: /health
    protocol: HTTP
    interval: 5s
    timeout: 2s
    healthyThreshold: 2
    unhealthyThreshold: 2
